fastapi>=0.110.0,<1.0.0
uvicorn>=0.29.0,<1.0.0
pydantic>=2.0.0,<3.0.0
httpx>=0.27.0,<1.0.0
requests>=2.32.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# Required by FastAPI's UploadFile/File(...) form-data parsing (used by the
# QB customer CSV import endpoint). Without this, importing the app raises:
# RuntimeError: Form data requires "python-multipart" to be installed.
python-multipart>=0.0.9,<1.0.0

# QuickBooks Desktop integration (QBFC via COM automation).
# Windows-only — the environment marker below means `pip install` skips this
# line entirely on Linux/macOS, so it's safe even though this repo is also
# read/edited from a Linux sandbox.
pywin32>=306; platform_system=="Windows"
